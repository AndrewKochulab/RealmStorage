## What this changes

<!-- What behaviour differs after this PR, and why. -->

## Why

<!-- The problem being solved. Link an issue if there is one. -->

## Checklist

- [ ] `swift test` passes
- [ ] `swift build -Xswiftc -warnings-as-errors` is clean
- [ ] `swiftlint --strict` is clean
- [ ] New behaviour has a test; fixed bugs have a test that failed before the fix
- [ ] Public API changes are documented, and `CHANGELOG.md` is updated
- [ ] Anything that changes on-disk layout or the schema is noted in `MIGRATION.md`

## Compatibility

<!-- Does this break source compatibility, on-disk format, or the minimum
     toolchain? If it breaks nothing, say so. -->
